# Optional WebSocket endpoint for local development.
# Leave unset to use the same-origin /ws/{room} endpoint after deployment.
# Example for `npm run worker:dev`:
VITE_MULTIPLAYER_URL=ws://localhost:8787
